Add options to SubWindow.

Database#update should support relative modification of the data, like UPDATE user SET balance = balance + 10

Database.Query#execute should implement Chain and use it like Fx with a link option.

Add missing events to Element.NativeEvents (dragstart, dragend, ...).

Easy way to update an application.

Some sound abstraction layer.
